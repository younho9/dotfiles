#!/usr/bin/env zsh

# Set Git language to English
alias git='LC_ALL=en_US git'

# Shortcuts
alias dl='cd ~/Downloads'
alias dt='cd ~/Desktop'
alias '$'=''

# claude - 전역 스킬/설정 제외하고 프로젝트 설정만 로드 (격리된 config dir 사용)
alias claude-clean='CLAUDE_CONFIG_DIR=~/.claude-clean claude'
